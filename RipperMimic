local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local localPlayer = Players.LocalPlayer
local blaSound

function light(tim, color0, color1)
	local info = TweenInfo.new(tim, Enum.EasingStyle.Linear)
	for _, light in pairs(game.Workspace.CurrentRooms:GetDescendants()) do
		if light:IsA("Light") or light:IsA("SurfaceLight") or light:IsA("SpotLight") then
			local target = {Color = color1}
			local anim = TweenService:Create(light, info, target)
			anim:Play()
		end
		if light:IsA("MeshPart") and light.Material == Enum.Material.Neon and light.Name ~= "Skybox" then
			local target1 = {Color = color0}
			local anim2 = TweenService:Create(light, info, target1)
			anim2:Play()
		end
	end
end

local function GetGitSound(GithubSnd, SoundName)
	local url = GithubSnd
	if not isfile(SoundName .. ".mp3") then
		writefile(SoundName .. ".mp3", game:HttpGet(url))
	end
	local sound = Instance.new("Sound")
	sound.SoundId = (getcustomasset or getsynasset)(SoundName .. ".mp3")
	return sound
end

local function FadeDestroy(model)
	if not model or not model.Parent then
		return
	end

	local info = TweenInfo.new(3, Enum.EasingStyle.Linear)

	for _, v in ipairs(model:GetDescendants()) do
		if v:IsA("Sound") then
			pcall(function()
				TweenService:Create(v, info, {
					PlaybackSpeed = 0
				}):Play()
			end)
		elseif v:IsA("ParticleEmitter") then
			v.Enabled = false
			pcall(function()
				TweenService:Create(v, info, {
					Rate = 0
				}):Play()
			end)
		elseif v:IsA("Beam") then
			pcall(function()
				TweenService:Create(v, info, {
					Brightness = 0,
					Transparency = NumberSequence.new(1)
				}):Play()
			end)
		elseif v:IsA("Trail") then
			pcall(function()
				TweenService:Create(v, info, {
					Transparency = NumberSequence.new(1)
				}):Play()
			end)
		elseif v:IsA("PointLight") or v:IsA("SpotLight") or v:IsA("SurfaceLight") then
			pcall(function()
				TweenService:Create(v, info, {
					Brightness = 0
				}):Play()
			end)
		end
	end

	task.delay(3, function()
		if model.Parent then
			model:Destroy()
		end
	end)
end

---====== Load spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/iccdjkkkkskskhdgdgegvbfbgncjjdudejdjvi/REBLAHARDCOREDI/refs/heads/main/blaspawn"))()

---====== Create entity ======---

local entity = Spawner:Create({
	Entity = {
		Name = "A60RipperLah",
		Asset = "https://github.com/lynguyen26031993-design/-u/raw/refs/heads/main/MultimonsterFace%20(1).rbxm?raw=true",
		HeightOffset = 4
	},
	Lights = {
		Flicker = {
			Enabled = false,
			Duration = 1
		},
		Shatter = true,
		Repair = false
	},
	Earthquake = {
		Enabled = false
	},
	CameraShake = {
		Enabled = true,
		Range = 100,
		Values = {10, 27, 0.1, 0.4}
	},
	Movement = {
		Speed = 180,
		Delay = 3,
		Reversed = false
	},
	Rebounding = {
		Enabled = false,
		Type = "Ambush",
		Min = 1,
		Max = 1,
		Delay = 2
	},
	Damage = {
		Enabled = true,
		Range = 40,
		Amount = 100
	},
	Crucifixion = {
		Enabled = true,
		Range = 40,
		Resist = false,
		Break = true
	},
	Death = {
		Type = "Guiding",
		Hints = {"Death", "Hints", "Go", "Here"},
		Cause = "Multi Monster"
	}
})

---====== Debug entity ======---

entity:SetCallback("OnSpawned", function()
	print("Entity has spawned")

	local _Sound = Instance.new('Sound')
	local bla = GetGitSound("https://github.com/iccdjkkkkskskhdgdgegvbfbgncjjdudejdjvi/REBLAHARDCOREDI/raw/refs/heads/main/Lhouijie%20Almendras%20-%20Ripper_a60%20-%201786206833758%202026-08-08%2020_33.mp3?raw=true", "kckvjjjjfjj")

	_Sound.SoundId = bla.SoundId
	_Sound.PlaybackSpeed = 1
	_Sound.Looped = false
	_Sound.Parent = workspace
	_Sound.Volume = 7

	bla:Destroy()
	_Sound:Play()

	light(4, Color3.fromRGB(85, 0, 0), Color3.fromRGB(255, 0, 0))

	local entityModel = workspace:FindFirstChild("A60RipperLah")
	local Multi = entityModel and entityModel:FindFirstChild("Multi")

	local Github = GetGitSound("https://github.com/cakmay227-svg/random/raw/refs/heads/main/XRecorder_Edited_20260801_01.mp3?raw=true", "jjjciie")

	local breaking = Instance.new("Sound")
	breaking.SoundId = Github.SoundId
	breaking.PlaybackSpeed = 1
	breaking.Parent = workspace
	breaking.Looped = false
	breaking.RollOffMaxDistance = 1000000000
	breaking.Volume = 4

	task.spawn(function()
		local conn
		conn = RunService.Heartbeat:Connect(function()
			local character = localPlayer.Character
			local hrp = character and character:FindFirstChild("HumanoidRootPart")

			if not hrp or not Multi or not breaking.Parent then
				if conn then
					conn:Disconnect()
				end
				return
			end

			local inRange = (Multi.Position - hrp.Position).Magnitude <= 150

			if inRange then
				if not breaking.IsPlaying then
					breaking:Play()
				end
			else
				if breaking.IsPlaying then
					breaking:Stop()
				end
			end
		end)

		breaking.Ended:Connect(function()
			if conn then
				conn:Disconnect()
			end
		end)
	end)

	task.spawn(function()

		local sound51 = Instance.new("Sound")
		sound51.PlaybackSpeed = 1
		sound51.Volume = 5
		sound51.SoundId = "rbxassetid://9125713501"
		sound51.Parent = workspace
		sound51:Play()

		task.wait(7)
		sound51:Destroy()
	end)
end)

entity:SetCallback("OnStartMoving", function()
	print("Entity has started moving")
	task.spawn(function()
		local gitSoundTemp2 = GetGitSound("https://github.com/lynguyen26031993-design/-u/raw/refs/heads/main/XRecorder_Edited_20260810_01.mp3?raw=true", "jjcjfjjc")

		blaSound = Instance.new("Sound") 
		blaSound.SoundId = gitSoundTemp2.SoundId
		blaSound.PlaybackSpeed = 1
		blaSound.Looped = false
		blaSound.TimePosition = 0
		blaSound.Volume = 1.5
		blaSound.RollOffMaxDistance = 150
		blaSound.Parent = workspace:WaitForChild("A60RipperLah"):WaitForChild("Multi")

		gitSoundTemp2:Destroy()
	end)
end)

local hasTriggered = false

entity:SetCallback("OnEnterRoom", function(room: Model, firstTime: boolean)
	local assetContainer = room:WaitForChild("Assets", 5)
	if assetContainer then
		task.spawn(function()
			task.wait(0.05)

			local breakSound = Instance.new("Sound")
			breakSound.SoundId = "rbxassetid://9114223171"
			breakSound.Volume = 0.2
			breakSound.Parent = room
			breakSound:Play()
			breakSound.Ended:Connect(function() breakSound:Destroy() end)

			local breakSounds = {
				"rbxassetid://126645609816652",
				"rbxassetid://130869638485735",
				"rbxassetid://7158347304",
				"rbxassetid://127327591850649",
				"rbxassetid://133653755194228",
				"rbxassetid://117158373103667",
				"rbxassetid://104568144438070"
			}

			local blacklist = {"wardrobe", "bed", "rug", "key", "drawer", "breaker", "table", "desk", "gold", "dresser"}

			for _, item in pairs(assetContainer:GetChildren()) do
				if item:IsA("Model") and item.Name ~= "Door" and item.Name ~= "HidingSpots" then
					local isBlacklisted = false
					local itemNameLower = string.lower(item.Name)
					for _, word in ipairs(blacklist) do
						if string.find(itemNameLower, word) then
							isBlacklisted = true
							break
						end
					end

					if not isBlacklisted then
						local primary = item.PrimaryPart or item:FindFirstChildWhichIsA("BasePart")

						if primary then
							task.spawn(function()
								for _, v in ipairs(item:GetDescendants()) do
									if v:IsA("BasePart") then
										local lowerName = string.lower(v.Name)

										if not string.find(lowerName, "floor") and not string.find(lowerName, "hitbox") then
											task.wait(math.random(1, 4) / 100)

											local randomSoundId = breakSounds[math.random(1, #breakSounds)]
											local s = Instance.new("Sound")
											s.SoundId = randomSoundId
											s.Volume = 0.4
											s.PlaybackSpeed = math.random(85, 115) / 100
											s.RollOffMaxDistance = 100
											s.Parent = v
											s:Play()

											s.Ended:Connect(function()
												s:Destroy()
											end)

											v.Anchored = false
											v.CanCollide = true 
											v.CustomPhysicalProperties = PhysicalProperties.new(0.01, 0.01, 0.5, 1, 1)

											pcall(function()
												if v:CanSetNetworkOwnership() then
													v:SetNetworkOwner(localPlayer)
												end
											end)

											v.CFrame = v.CFrame + Vector3.new(0, 0.05, 0)
											v.AssemblyLinearVelocity = Vector3.new(math.random(-120, 120), math.random(40, 90), math.random(-120, 120))
											v.AssemblyAngularVelocity = Vector3.new(math.random(-70, 70), math.random(-70, 70), math.random(-70, 70))

											local mass = v:GetMass()
											v:ApplyImpulse(Vector3.new(math.random(-120, 120), math.random(60, 120), math.random(-120, 120)) * mass)
											v:ApplyAngularImpulse(Vector3.new(math.random(-60, 60), math.random(-60, 60), math.random(-60, 60)) * mass)
										end
									end
								end
							end)

							primary.Anchored = true
							task.wait()
							primary.Anchored = false
							primary.AssemblyLinearVelocity = Vector3.new(math.random(-140, 140), math.random(60, 110), math.random(-140, 140))
							primary.AssemblyAngularVelocity = Vector3.new(math.random(-80, 80), math.random(-80, 80), math.random(-80, 80))
						end
					end
				end
			end
		end)
	end

	local latestRoomNum = tostring(ReplicatedStorage.GameData.LatestRoom.Value)
	if room.Name ~= latestRoomNum then 
		return 
	end

	if hasTriggered then 
		return 
	end

	local entityModel = workspace:FindFirstChild("A60RipperLah")
	local entityRoot = entityModel and (entityModel:FindFirstChild("HumanoidRootPart") or entityModel:FindFirstChildWhichIsA("BasePart")) 
	local doorModel = room:FindFirstChild("Door")
	local doorPanel = doorModel and doorModel:FindFirstChild("Door")

	if entityModel and entityRoot and doorPanel and doorPanel:IsA("BasePart") then
		hasTriggered = true

		while entityModel and entityModel.Parent and entityRoot and (entityRoot.Position - doorPanel.Position).Magnitude > 8 do
			task.wait()
		end

		if entityModel and entityModel.Parent and entityRoot then
			local doorFreezePos = entityRoot.CFrame
			local freezeConnection

			freezeConnection = RunService.Heartbeat:Connect(function()
				if entityModel and entityModel.Parent and entityRoot then
					entityRoot.Anchored = true
					entityRoot.CFrame = doorFreezePos
					entityRoot.AssemblyLinearVelocity = Vector3.zero
					entityRoot.AssemblyAngularVelocity = Vector3.zero
				else
					if freezeConnection then
						freezeConnection:Disconnect()
					end
				end
			end)

			local chance = 2
			for _, v in ipairs(room:GetDescendants()) do
				if v:IsA("BasePart") and v.Name == "KeyHitbox" then
					chance = 1
					break
				end
			end

			task.spawn(function()
				local CameraShaker = require(ReplicatedStorage:WaitForChild("CameraShaker"))
				local camera = workspace.CurrentCamera

				if chance == 1 then
					task.wait(2)
				else
					if blaSound then
						blaSound:Play() 
					end
					
					local originalCFrame = entityRoot.CFrame				
					local backCFrame = originalCFrame * CFrame.new(0, 0, 25) 
					local tweenBackInfo = TweenInfo.new(2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
					local tweenBack = TweenService:Create(entityRoot, tweenBackInfo, {CFrame = backCFrame})

					tweenBack:Play()
					tweenBack.Completed:Wait()

					local tweenForwardInfo = TweenInfo.new(0.5, Enum.EasingStyle.Quad, Enum.EasingDirection.In)
					local tweenForward = TweenService:Create(entityRoot, tweenForwardInfo, {CFrame = originalCFrame})
					tweenForward:Play()
					task.wait(0.45)

					local camShake2 = CameraShaker.new(Enum.RenderPriority.Camera.Value, function(shakeCf)
						camera.CFrame = camera.CFrame * shakeCf
					end)
					camShake2:Start()
					camShake2:ShakeOnce(30, 11.5, 0.1, 2, 15, 0.5) 

					if entityRoot:FindFirstChild("DoorOpen1") then
						entityRoot.DoorOpen1.Volume = 5
						entityRoot.DoorOpen1:Play()	
					end
					
					local CurrentRooms = workspace:WaitForChild("CurrentRooms")
					local LatestRoom = ReplicatedStorage:WaitForChild("GameData"):WaitForChild("LatestRoom")
					local currentRoom = CurrentRooms:FindFirstChild(tostring(LatestRoom.Value))
					if currentRoom then
						for _, v in ipairs(currentRoom:GetDescendants()) do
							if v:IsA("BasePart") and v.Name == "KeyHitbox" then
								if freezeConnection then freezeConnection:Disconnect() end
								return
							end
						end
					end

					local targetDoorModel
					local function HandleHidden(model)
						local found = false
						for _, v in ipairs(model:GetDescendants()) do
							if v:IsA("BasePart") and v.Name == "Hidden" then
								v.Transparency = 1
								found = true
							end
						end

						if not found then
							model.DescendantAdded:Connect(function(v)
								if v:IsA("BasePart") and v.Name == "Hidden" then
									v.Transparency = 1
								end
							end)
						end
					end

					if currentRoom then
						for _, v in ipairs(currentRoom:GetDescendants()) do
							if v:IsA("Model") and v.Name == "Door" then
								targetDoorModel = v
								break
							end
						end
					end

					if targetDoorModel then
						HandleHidden(targetDoorModel)

						local cloneModel = targetDoorModel:Clone()
						cloneModel.Name = "DoorClone"
						cloneModel.Parent = targetDoorModel.Parent

						local cloneDoor
						for _, v in ipairs(cloneModel:GetDescendants()) do
							if v:IsA("MeshPart") and v.Name == "Door" then
								cloneDoor = v
								break
							end
						end

						if cloneDoor then
							cloneDoor.CFrame = cloneDoor.CFrame * CFrame.Angles(math.rad(-90), 0, 0) * CFrame.new(0, 7, -3.7)

							for _, v in ipairs(targetDoorModel:GetDescendants()) do
								if v:IsA("BasePart") then
									v.Transparency = 1
								elseif v:IsA("SurfaceGui") and v.Name == "Sign" then
									v:Destroy()
								elseif v:IsA("Sound") then
									v.Volume = 0
								end
							end

							HandleHidden(cloneModel)
						else
							cloneModel:Destroy()
						end
					end
				end

				if blaSound and blaSound.IsPlaying then
					blaSound.Ended:Wait()
				end
				
				if freezeConnection then
					freezeConnection:Disconnect()
				end

				FadeDestroy(entityModel)
				hasTriggered = false 
			end)
		end
	end
end)

entity:SetCallback("OnLookAt", function(lineOfSight: boolean)
	if lineOfSight == true then
		print("Player is looking at entity")
	else
		print("Player view is obstructed by something")
	end
end)

entity:SetCallback("OnRebounding", function(startOfRebound: boolean)
	if startOfRebound == true then
		print("Entity has started rebounding")
	else
		print("Entity has finished rebounding")
	end
end)

entity:SetCallback("OnDespawning", function()
	print("Entity is despawning")
end)

entity:SetCallback("OnDespawned", function()
	print("Entity has despawned")
end)

entity:SetCallback("OnDamagePlayer", function(newHealth)
	if newHealth == 0 then
		print("Entity has killed the player")
		getgenv().MultiKilled = true
		
		local PlayerGui = localPlayer:WaitForChild("PlayerGui")
		local Remotes = ReplicatedStorage:WaitForChild("RemotesFolder")

		local DeathTypes = {
			["Yellow"] = {"yellow", "curious"},
			["Blue"] = {"blue", "guiding"}
		}

		local function DeathHint(hints, deathType)
			if typeof(hints) ~= "table" or #hints == 0 then
				return
			end

			local MainUI = PlayerGui:WaitForChild("MainUI")
			local Death = MainUI:WaitForChild("Death")

			Death:GetPropertyChangedSignal("Visible"):Wait()
			deathType = tostring(deathType or "Guiding"):lower()

			local colour
			for name, values in next, DeathTypes do
				if table.find(values, deathType) then
					colour = name
					break
				end
			end

			if not colour then
				local MainGame = MainUI:WaitForChild("Initiator"):WaitForChild("Main_Game")
				local Health = MainGame:WaitForChild("Health")
				local Music = Health:WaitForChild("Music")

				for _, c in next, Music:GetChildren() do
					if c.Name:lower() == deathType then
						colour = c.Name
						break
					end
				end
			end

			colour = colour or "Blue"

			if firesignal then
				firesignal(Remotes.DeathHint.OnClientEvent, hints, colour)
			end
		end

		DeathHint({"", "", "", "", "", "", "", ""}, "Guiding")
	else
		print("Entity has damaged the player")
	end
end)

---====== Run entity ======---

entity:Run(true)
