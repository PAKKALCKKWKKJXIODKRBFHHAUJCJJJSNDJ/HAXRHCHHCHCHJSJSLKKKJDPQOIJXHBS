
local ReplicatedStorage = game:GetService("ReplicatedStorage") 

local function GetGitSound(GithubSnd, SoundName)
			local url = GithubSnd
			if not isfile(SoundName .. ".mp3") then
				writefile(SoundName .. ".mp3", game:HttpGet(url))
			end
			local sound = Instance.new("Sound")
			sound.SoundId = (getcustomasset or getsynasset)(SoundName .. ".mp3")
			return sound
		end
	

function light(tim,color0,color1)
	local tweenservice = game:GetService("TweenService")
	local info = TweenInfo.new(tim,Enum.EasingStyle.Linear)
	for _ , light in pairs(game.Workspace.CurrentRooms:GetDescendants()) do
		if light:IsA("Light") or light:IsA("SurfaceLight") or light:IsA("SpotLight") then
			local target = {Color = color1}
			local anim = tweenservice:Create(light,info,target)
			anim:Play()
		end
		if light:IsA("MeshPart") and light.Material == Enum.Material.Neon  and light.Name ~= "Skybox" then
			local target1 = {Color = color0}
			local anim2 = tweenservice:Create(light,info,target1)
			anim2:Play()
		end
	end
end

task.spawn(function()
	light(4, Color3.fromRGB(0, 0, 255), Color3.fromRGB(0, 0, 255))
end)

task.wait(5)

local function GetGitSound(GithubSnd, SoundName)
					local url = GithubSnd
					if not isfile(SoundName .. ".mp3") then
						writefile(SoundName .. ".mp3", game:HttpGet(url))
					end
					local sound = Instance.new("Sound")
					sound.SoundId = (getcustomasset or getsynasset)(SoundName .. ".mp3")
					return sound
				end
			

local TweenService = game:GetService("TweenService")

local function FadeDestroy(model)
	if not model or not model.Parent then
		return
	end

	local info = TweenInfo.new(3, Enum.EasingStyle.Linear)

	for _, v in ipairs(model:GetDescendants()) do
		if v:IsA("Sound") then
			pcall(function()
				TweenService:Create(v, info, {
					PlaybackSpeed = 0
				}):Play()
			end)

		elseif v:IsA("ParticleEmitter") then
			v.Enabled = false
			pcall(function()
				TweenService:Create(v, info, {
					Rate = 0
				}):Play()
			end)

		elseif v:IsA("Beam") then
			pcall(function()
				TweenService:Create(v, info, {
					Brightness = 0,
					Transparency = NumberSequence.new(1)
				}):Play()
			end)

		elseif v:IsA("Trail") then
			pcall(function()
				TweenService:Create(v, info, {
					Transparency = NumberSequence.new(1)
				}):Play()
			end)

		elseif v:IsA("PointLight")
			or v:IsA("SpotLight")
			or v:IsA("SurfaceLight") then

			pcall(function()
				TweenService:Create(v, info, {
					Brightness = 0
				}):Play()
			end)
		end
	end

	task.delay(3, function()
		if model.Parent then
			model:Destroy()
		end
	end)
end

---====== Load spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/PAKKALCKKWKKJXIODKRBFHHAUJCJJJSNDJ/HAXRHCHHCHCHJSJSLKKKJDPQOIJXHBS/refs/heads/main/PrimeRipperSpawn"))()

---====== Create entity ======---

local entity = Spawner:Create({
	Entity = {
		Name = "PrimeRipperBla",
		Asset = "https://github.com/lynguyen26031993-design/-u/raw/refs/heads/main/Place_131351567799504_Model_Custom_PrimeRipper_1789044891.txt",
		HeightOffset = 4
	},
	Lights = {
		Flicker = {
			Enabled = false,
			Duration = 1
		},
		Shatter = false,
		Repair = false
	},
	Earthquake = {
		Enabled = false
	},
	CameraShake = {
		Enabled = true,
		Range = 100,
		Values = {10, 40, 1, 0.4} -- Magnitude, Roughness, FadeIn, FadeOut
	},
	Movement = {
		Speed = 400,
		Delay = 0,
		Reversed = true
	},
	Rebounding = {
		Enabled = false,
		Type = "Ambush", -- "Blitz"
		Min = 1,
		Max = 1,
		Delay = 2
	},
	Damage = {
		Enabled = true,
		Range = 50,
		Amount = 100
	},
	Crucifixion = {
		Enabled = false,
		Range = 40,
		Resist = false,
		Break = true
	},
	Death = {
		Type = "Guiding", -- "Curious"
		Hints = {"Death", "Hints", "Go", "Here"},
		Cause = "Prime Ripper"
	}
})

---====== Debug entity ======---

entity:SetCallback("OnSpawned", function()
	print("Entity has spawned")
	
	local entityModel = entity.Model
		local ripe = entityModel and (entityModel:FindFirstChild("Ripe") or entityModel:FindFirstChild("ripe")) 
	
	local Github = GetGitSound("https://github.com/cakmay227-svg/random/raw/refs/heads/main/XRecorder_Edited_20260801_01.mp3?raw=true", "jjjciie")

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")

local breaking = Instance.new("Sound")
breaking.SoundId = Github.SoundId
breaking.PlaybackSpeed = 1
breaking.Parent = workspace
breaking.Looped = false
breaking.RollOffMaxDistance = 1000000000
breaking.Volume = 0

local player = Players.LocalPlayer

task.spawn(function()
	local conn

	conn = RunService.Heartbeat:Connect(function()
		local character = player.Character
		local hrp = character and character:FindFirstChild("HumanoidRootPart")

		if not hrp or not ripe or not breaking.Parent then
			if conn then
				conn:Disconnect()
			end
			return
		end

		local inRange = (ripe.Position - hrp.Position).Magnitude <= 100

		if inRange then
			if not breaking.IsPlaying then
				breaking:Play()
			end
		else
			if breaking.IsPlaying then
				breaking:Stop()
			end
		end
	end)

	breaking.Ended:Connect(function()
		if conn then
			conn:Disconnect()
		end
	end)
end)
end)

entity:SetCallback("OnStartMoving", function()
	print("Entity has started moving")


end)

local hasTriggered = false

entity:SetCallback("OnEnterRoom", function(room: Model, firstTime: boolean)
	-- 1. Xử lý hiệu ứng nổ tung các mảnh nội thất trong phòng (Giữ nguyên của bạn)


	-- Kiểm tra tên phòng xem có trùng khớp không
	local latestRoomNum = tostring(game.ReplicatedStorage.GameData.LatestRoom.Value)
	if room.Name ~= latestRoomNum then 
		return 
	end

	-- Nếu đã kích hoạt chuỗi hiệu ứng rồi thì bỏ qua để tránh spam
	if hasTriggered then 
		return 
	end

	local RunService = game:GetService("RunService")
	local TweenService = game:GetService("TweenService")

	local entityModel = entity.Model
	local entityRoot = entityModel and (entityModel:FindFirstChild("HumanoidRootPart") or entityModel:FindFirstChildWhichIsA("BasePart")) 

local Rooms = workspace.CurrentRooms

local Room
local SmallestNum = math.huge

for _, v in ipairs(Rooms:GetChildren()) do
	local Num = tonumber(v.Name)

	if Num and Num < SmallestNum then
		SmallestNum = Num
		Room = v
	end
end

	local doorPanel = Room:FindFirstChild("RoomEntrance")

	if entityModel and entityRoot and doorPanel and doorPanel:IsA("BasePart") then
		hasTriggered = true

		while entityModel and entityModel.Parent and entityRoot and (entityRoot.Position - doorPanel.Position).Magnitude > 5 do
			task.wait()
		end
		
		hasTriggered = true
				
				FadeDestroy(entityModel)
				end
end)

entity:SetCallback("OnLookAt", function(lineOfSight: boolean)
	if lineOfSight == true then
		print("Player is looking at entity")
	else
		print("Player view is obstructed by something")
	end
end)

entity:SetCallback("OnRebounding", function(startOfRebound: boolean)
	if startOfRebound == true then
		print("Entity has started rebounding")
	else
		print("Entity has finished rebounding")
	end
end)

entity:SetCallback("OnDespawning", function()
	print("Entity is despawning")
end)

entity:SetCallback("OnDespawned", function()
	print("Entity has despawned")
end)

entity:SetCallback("OnDamagePlayer", function(newHealth)
	if newHealth == 0 then
		print("Entity has killed the player")
	else
		print("Entity has damaged the player")
	end
end)


--[[

DEVELOPER NOTE:
By overwriting 'CrucifixionOverwrite' the default crucifixion callback will be replaced with your custom callback.

entity:SetCallback("CrucifixionOverwrite", function()
    print("Custom crucifixion callback")
end)

]]--

---====== Run entity ======---

entity:Run(true)
