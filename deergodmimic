if getgenv().spam then
local backward = false

local function Deer() 
---====== Load spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/PAKKALCKKWKKJXIODKRBFHHAUJCJJJSNDJ/HAXRHCHHCHCHJSJSLKKKJDPQOIJXHBS/refs/heads/main/deermimicspawn"))()

local function GetGitSound(GithubSnd, SoundName)
			local url = GithubSnd
			if not isfile(SoundName .. ".mp3") then
				writefile(SoundName .. ".mp3", game:HttpGet(url))
			end
			local sound = Instance.new("Sound")
			sound.SoundId = (getcustomasset or getsynasset)(SoundName .. ".mp3")
			return sound
		end
		
		local gitSoundTemp2 = GetGitSound("https://github.com/lynguyen26031993-design/-u/raw/refs/heads/main/youtube_videoWithAudio.mp3?raw=true", "pcpkdkckkdknc")

local blaSound = Instance.new("Sound") 
		blaSound.SoundId = gitSoundTemp2.SoundId
		blaSound.PlaybackSpeed = 1
		blaSound.Looped = false
		blaSound.TimePosition = 5
		blaSound.Volume = 1
		blaSound.RollOffMaxDistance = 50

-- Biến cờ để kiểm soát việc dừng chớp toàn bộ

-- 2. Tự động áp dụng cho bất kỳ phòng MỚI nào vừa được thêm và

---====== Create entity ======---

local entity = Spawner:Create({
	Entity = {
		Name = "A60ButDeer",
		Asset = "https://github.com/lynguyen26031993-design/-u/raw/refs/heads/main/Multimonster%20(1).rbxm",
		HeightOffset = 0
	},
	Lights = {
		Flicker = {
			Enabled = false,
			Duration = 4
		},
		Shatter = false,
		Repair = false
	},
	Earthquake = {
		Enabled = false
	},
	CameraShake = {
		Enabled = false,
		Range = 150,
		Values = {3, 10, 1, 0.1} -- Magnitude, Roughness, FadeIn, FadeOut
	},
	Movement = {
		Speed = 30,
		Delay = 0,
		Reversed = backward
	},
	Rebounding = {
		Enabled = false,
		Type = "Ambush", -- "Blitz"
		Min = 1,
		Max = 1,
		Delay = 0
	},
	Damage = {
		Enabled = true,
		Range = 15,
		Amount = 125
	},
	Crucifixion = {
		Enabled = false,
		Range = 40,
		Resist = false,
		Break = true
	},
	Death = {
		Type = "Guiding", -- "Curious"
		Hints = {"Death", "Hints", "Go", "Here"},
		Cause = "Multi Monster"
	}
})

---====== Debug entity ======---

entity:SetCallback("OnSpawned", function()
	local Model = entity.Model
	local partneeded = Model:FindFirstChild("Multi")

	-- Load CensorBox
	local CensorURL = "https://github.com/lynguyen26031993-design/-u/raw/refs/heads/main/Place_6839171747_BillboardGui_MonsterGui_1789749680.txt"
	local CensorFile = "CensorBox.txt"

	if not isfile(CensorFile) then
		writefile(CensorFile, game:HttpGet(CensorURL))
	end

	local censor = game:GetObjects(getcustomasset(CensorFile))[1]

	if censor and partneeded then
		censor.Parent = partneeded
censor:FindFirstChildWhichIsA("Frame").BorderColor3 = Color3.fromRGB(0, 0, 0)

		censor.StudsOffset = censor.StudsOffset - Vector3.new(0, 3, 0)
	end

	-- Mute toàn bộ Sound trong entity
	for _, Obj in ipairs(Model:GetDescendants()) do
		if Obj:IsA("Sound") then
			Obj.Volume = 0
			elseif Obj:IsA("PointLight") then
			Obj.Range = 10
		end
	end
	
		blaSound.Parent = partneeded
	blaSound:Play()

	-- Load SoundBox
	local SoundURL = "https://github.com/lynguyen26031993-design/-u/raw/refs/heads/main/Place_6839171747_Part_SoundBox_1789752252.txt"
	local SoundFile = "SoundBox.txt"

	if not isfile(SoundFile) then
		writefile(SoundFile, game:HttpGet(SoundURL))
	end

	local SoundBox = game:GetObjects(getcustomasset(SoundFile))[1]

	if not SoundBox then
		return
	end

	-- Tìm BasePart trong entity
	local Target = Model:FindFirstChildWhichIsA("BasePart", true)

	if not Target then
		SoundBox:Destroy()
		return
	end

	-- Đưa SoundBox vào entity
	SoundBox.Parent = Target

	-- TP SoundBox tới BasePart tìm được
	if SoundBox:IsA("Model") then
		SoundBox:PivotTo(Target.CFrame)
	elseif SoundBox:IsA("BasePart") then
		SoundBox.CFrame = Target.CFrame
	end

	-- Weld SoundBox vào Target
	if SoundBox:IsA("Model") then
		for _, Obj in ipairs(SoundBox:GetDescendants()) do
			if Obj:IsA("BasePart") then
				Obj.Anchored = false

				local Weld = Instance.new("WeldConstraint")
				Weld.Part0 = Target
				Weld.Part1 = Obj
				Weld.Parent = Obj
			end
		end
	elseif SoundBox:IsA("BasePart") then
		SoundBox.Anchored = false

		local Weld = Instance.new("WeldConstraint")
		Weld.Part0 = Target
		Weld.Part1 = SoundBox
		Weld.Parent = SoundBox
	end
end)

entity:SetCallback("OnStartMoving", function()
end)

entity:SetCallback("OnEnterRoom", function(room: Model, firstTime: boolean)
    if firstTime then
    else
    end
end)

entity:SetCallback("OnLookAt", function(lineOfSight: boolean)
	if lineOfSight == true then
	else
	end
end)

entity:SetCallback("OnRebounding", function(startOfRebound: boolean)
    if startOfRebound == true then
        print("Entity has started rebounding")
	else
        print("Entity has finished rebounding")
	end
end)

entity:SetCallback("OnDespawning", function()
    print("Entity is despawning")
end)

entity:SetCallback("OnDespawned", function()
    print("Entity has despawned")
end)

entity:SetCallback("OnDamagePlayer", function(newHealth: number)
	if newHealth <= 0 then
		print("Entity has killed the player")
		local Players = game:GetService("Players")
		getgenv().MultiKilled = true
		
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")
local Remotes = ReplicatedStorage:WaitForChild("RemotesFolder")

local DeathTypes = {
	["Yellow"] = {"yellow", "curious"},
	["Blue"] = {"blue", "guiding"}
}

local function DeathHint(hints, deathType)
	if typeof(hints) ~= "table" or #hints == 0 then
		return
	end

	local MainUI = PlayerGui:WaitForChild("MainUI")
	local Death = MainUI:WaitForChild("Death")

	Death:GetPropertyChangedSignal("Visible"):Wait()

	deathType = tostring(deathType or "Guiding"):lower()

	local colour

	for name, values in next, DeathTypes do
		if table.find(values, deathType) then
			colour = name
			break
		end
	end

	if not colour then
		local MainGame = MainUI:WaitForChild("Initiator"):WaitForChild("Main_Game")
		local Health = MainGame:WaitForChild("Health")
		local Music = Health:WaitForChild("Music")

		for _, c in next, Music:GetChildren() do
			if c.Name:lower() == deathType then
				colour = c.Name
				break
			end
		end
	end

	colour = colour or "Blue"

	if firesignal then
		firesignal(
			Remotes.DeathHint.OnClientEvent,
			hints,
			colour
		)
	end
end

DeathHint({
"", "", "", "", "", "", "", ""
}, "Guiding")
	else
		print("Entity has damaged the player")
	end
end)

--[[

DEVELOPER NOTE:
By overwriting 'CrucifixionOverwrite' the default crucifixion callback will be replaced with your custom callback.

entity:SetCallback("CrucifixionOverwrite", function()
    print("Custom crucifixion callback")
end)

]]--

---====== Run entity ======---

entity:Run(true)
end

if math.random(1, 100) > 30 then
    task.spawn(Deer) 

else

backward = true

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local CurrentRooms = workspace:WaitForChild("CurrentRooms")
local LatestRoom = ReplicatedStorage.GameData:WaitForChild("LatestRoom")

local URL = "https://github.com/lynguyen26031993-design/-u/raw/refs/heads/main/Place_131351567799504_Model_A60Real_1789050841.txt"
local File = "A60Real.txt"

if not isfile(File) then
	writefile(File, game:HttpGet(URL))
end

local function GetGitSound(GithubSnd, SoundName)
			local url = GithubSnd
			if not isfile(SoundName .. ".mp3") then
				writefile(SoundName .. ".mp3", game:HttpGet(url))
			end
			local sound = Instance.new("Sound")
			sound.SoundId = (getcustomasset or getsynasset)(SoundName .. ".mp3")
			return sound
		end
		
		local gitSoundTemp2 = GetGitSound("https://github.com/lynguyen26031993-design/-u/raw/refs/heads/main/7ba215ed-8cb5-48c0-9274-fd0fad7bdbfc.mp3?raw=true", "ipljjhgfffgv")

local blaSound = Instance.new("Sound") 
		blaSound.SoundId = gitSoundTemp2.SoundId
		blaSound.PlaybackSpeed = 1
		blaSound.Looped = false
		blaSound.TimePosition = 0
		blaSound.Volume = 2
		blaSound.RollOffMaxDistance = 300

local Model = game:GetObjects(getcustomasset(File))[1]

if Model then
	Model.Parent = workspace
	blaSound.Parent = workspace 

	local Room = CurrentRooms:FindFirstChild(tostring(LatestRoom.Value))

	if Room then
		local RoomEntrance = Room:FindFirstChild("RoomEntrance")

		if RoomEntrance and RoomEntrance:IsA("BasePart") then
			local TargetCFrame =
				RoomEntrance.CFrame
				+ RoomEntrance.CFrame.LookVector * 10

			local CFrameValue = Instance.new("CFrameValue")
			CFrameValue.Value = Model:GetPivot()

			local Connection = CFrameValue:GetPropertyChangedSignal("Value"):Connect(function()
				if Model and Model.Parent then
					Model:PivotTo(CFrameValue.Value)
				end
			end)

			local Tween = TweenService:Create(
				CFrameValue,
				TweenInfo.new(0.5, Enum.EasingStyle.Linear),
				{Value = TargetCFrame}
			)

			Tween:Play()
			Tween.Completed:Wait()
			
			local CameraShaker = require(game.ReplicatedStorage:WaitForChild("CameraShaker"))
local camera = workspace.CurrentCamera

local spawnShake = CameraShaker.new(Enum.RenderPriority.Camera.Value, function(shakeCf)
	camera.CFrame = camera.CFrame * shakeCf
end)

spawnShake:Start()

spawnShake:ShakeOnce(
	5,   -- Magnitude (mạnh)
	3,   -- Roughness
	0.05, -- FadeIn
	3, -- FadeOut
	0.8,    -- PositionInfluence
	0.8   -- RotationInfluence
)

			Connection:Disconnect()
			CFrameValue:Destroy()

			local Names = {
				A60ButDeer = true
			}

			local TargetModel

			local Connection2 = workspace.ChildAdded:Connect(function(Obj)
				if not TargetModel
					and Obj:IsA("Model")
					and Names[Obj.Name] then

					TargetModel = Obj
				end
			end)
			
		 task.delay(1, function()
    Deer()
end)

			repeat
				task.wait()
			until TargetModel

			Connection2:Disconnect()

			local TargetPart

			for _, Obj in ipairs(TargetModel:GetDescendants()) do
				if Obj:IsA("BasePart") then
					TargetPart = Obj
					break
				end
			end

			if TargetPart then
				local CFrameValue2 = Instance.new("CFrameValue")
				CFrameValue2.Value = Model:GetPivot()

				local Connection3 = CFrameValue2:GetPropertyChangedSignal("Value"):Connect(function()
					if Model and Model.Parent then
						Model:PivotTo(CFrameValue2.Value)
					end
				end)

				local Tween2 = TweenService:Create(
					CFrameValue2,
					TweenInfo.new(0.5, Enum.EasingStyle.Linear),
					{Value = TargetPart.CFrame}
				)

spawnShake:ShakeOnce(
	7,   -- Magnitude (mạnh)
	3,   -- Roughness
	0.05, -- FadeIn
	3, -- FadeOut
	0.8,    -- PositionInfluence
	0.8   -- RotationInfluence
)

blaSound:Play()

				Tween2:Play()
				Tween2.Completed:Wait()

				Connection3:Disconnect()
				CFrameValue2:Destroy()

				Model:Destroy()
			end
		end
	end
end
end
else
---====== Load spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/PAKKALCKKWKKJXIODKRBFHHAUJCJJJSNDJ/HAXRHCHHCHCHJSJSLKKKJDPQOIJXHBS/refs/heads/main/deermimicspawn"))()

local function GetGitSound(GithubSnd, SoundName)
			local url = GithubSnd
			if not isfile(SoundName .. ".mp3") then
				writefile(SoundName .. ".mp3", game:HttpGet(url))
			end
			local sound = Instance.new("Sound")
			sound.SoundId = (getcustomasset or getsynasset)(SoundName .. ".mp3")
			return sound
		end
		
		local gitSoundTemp2 = GetGitSound("https://github.com/lynguyen26031993-design/-u/raw/refs/heads/main/youtube_videoWithAudio.mp3?raw=true", "pcpkdkckkdknc")

local blaSound = Instance.new("Sound") 
		blaSound.SoundId = gitSoundTemp2.SoundId
		blaSound.PlaybackSpeed = 1
		blaSound.Looped = false
		blaSound.TimePosition = 5
		blaSound.Volume = 1
		blaSound.RollOffMaxDistance = 50

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local CurrentRooms = workspace:WaitForChild("CurrentRooms")
local Module_Events = require(ReplicatedStorage.ModulesClient.Module_Events)

-- Biến cờ để kiểm soát việc dừng chớp toàn bộ
getgenv().IsAllRoomsFlickering = true

-- Hàm cho 1 phòng chớp đèn liên tục
local function FlickerRoomLoop(room)
    task.spawn(function()
        while getgenv().IsAllRoomsFlickering and room and room.Parent do
            if room:IsA("Model") then
                Module_Events.flicker(room, 1)
            end
            task.wait(1) -- Thời gian chờ giữa các lần chớp của phòng đó
        end
    end)
end

-- 1. Áp dụng cho các phòng đã có sẵn từ trước
for _, room in next, CurrentRooms:GetChildren() do
    if room:IsA("Model") then
        FlickerRoomLoop(room)
    end
end

-- 2. Tự động áp dụng cho bất kỳ phòng MỚI nào vừa được thêm vào
CurrentRooms.ChildAdded:Connect(function(room)
    if room:IsA("Model") and getgenv().IsAllRoomsFlickering then
        -- Đợi một chút để phòng load xong các thành phần bên trong
        task.wait(0.5)
        FlickerRoomLoop(room)
        print("Đã thêm phòng mới vào danh sách chớp đèn:", room.Name)
    end
end)

print("Đã bật hệ thống chớp đèn tự động cho tất cả phòng (cũ và mới)!")


-- \\ Phần code ngừng chớp (Chạy dòng này khi muốn dừng toàn bộ) // --
-- getgenv().IsAllRoomsFlickering = false

---====== Create entity ======---

local entity = Spawner:Create({
	Entity = {
		Name = "A60ButDeer",
		Asset = "https://github.com/lynguyen26031993-design/-u/raw/refs/heads/main/Multimonster%20(1).rbxm",
		HeightOffset = 0
	},
	Lights = {
		Flicker = {
			Enabled = false,
			Duration = 4
		},
		Shatter = false,
		Repair = false
	},
	Earthquake = {
		Enabled = false
	},
	CameraShake = {
		Enabled = false,
		Range = 150,
		Values = {3, 10, 1, 0.1} -- Magnitude, Roughness, FadeIn, FadeOut
	},
	Movement = {
		Speed = 30,
		Delay = 0,
		Reversed = false
	},
	Rebounding = {
		Enabled = false,
		Type = "Ambush", -- "Blitz"
		Min = 1,
		Max = 1,
		Delay = 0
	},
	Damage = {
		Enabled = true,
		Range = 15,
		Amount = 125
	},
	Crucifixion = {
		Enabled = false,
		Range = 40,
		Resist = false,
		Break = true
	},
	Death = {
		Type = "Guiding", -- "Curious"
		Hints = {"Death", "Hints", "Go", "Here"},
		Cause = "Multi Monster"
	}
})

---====== Debug entity ======---

entity:SetCallback("OnSpawned", function()
	local Model = entity.Model
	local partneeded = Model:FindFirstChild("Multi")

	-- Load CensorBox
	local CensorURL = "https://github.com/lynguyen26031993-design/-u/raw/refs/heads/main/Place_6839171747_BillboardGui_MonsterGui_1789749680.txt"
	local CensorFile = "CensorBox.txt"

	if not isfile(CensorFile) then
		writefile(CensorFile, game:HttpGet(CensorURL))
	end

	local censor = game:GetObjects(getcustomasset(CensorFile))[1]

	if censor and partneeded then
		censor.Parent = partneeded
censor:FindFirstChildWhichIsA("Frame").BorderColor3 = Color3.fromRGB(0, 0, 0)

		censor.StudsOffset = censor.StudsOffset - Vector3.new(0, 3, 0)
	end

	-- Mute toàn bộ Sound trong entity
	for _, Obj in ipairs(Model:GetDescendants()) do
		if Obj:IsA("Sound") then
			Obj.Volume = 0
			elseif Obj:IsA("PointLight") then
			Obj.Range = 10
		end
	end
	
		blaSound.Parent = partneeded
	blaSound:Play()

	-- Load SoundBox
	local SoundURL = "https://github.com/lynguyen26031993-design/-u/raw/refs/heads/main/Place_6839171747_Part_SoundBox_1789752252.txt"
	local SoundFile = "SoundBox.txt"

	if not isfile(SoundFile) then
		writefile(SoundFile, game:HttpGet(SoundURL))
	end

	local SoundBox = game:GetObjects(getcustomasset(SoundFile))[1]

	if not SoundBox then
		return
	end

	-- Tìm BasePart trong entity
	local Target = Model:FindFirstChildWhichIsA("BasePart", true)

	if not Target then
		SoundBox:Destroy()
		return
	end

	-- Đưa SoundBox vào entity
	SoundBox.Parent = Target

	-- TP SoundBox tới BasePart tìm được
	if SoundBox:IsA("Model") then
		SoundBox:PivotTo(Target.CFrame)
	elseif SoundBox:IsA("BasePart") then
		SoundBox.CFrame = Target.CFrame
	end

	-- Weld SoundBox vào Target
	if SoundBox:IsA("Model") then
		for _, Obj in ipairs(SoundBox:GetDescendants()) do
			if Obj:IsA("BasePart") then
				Obj.Anchored = false

				local Weld = Instance.new("WeldConstraint")
				Weld.Part0 = Target
				Weld.Part1 = Obj
				Weld.Parent = Obj
			end
		end
	elseif SoundBox:IsA("BasePart") then
		SoundBox.Anchored = false

		local Weld = Instance.new("WeldConstraint")
		Weld.Part0 = Target
		Weld.Part1 = SoundBox
		Weld.Parent = SoundBox
	end
end)

entity:SetCallback("OnStartMoving", function()
end)

entity:SetCallback("OnEnterRoom", function(room: Model, firstTime: boolean)
    if firstTime then
    else
    end
end)

entity:SetCallback("OnLookAt", function(lineOfSight: boolean)
	if lineOfSight == true then
	else
	end
end)

entity:SetCallback("OnRebounding", function(startOfRebound: boolean)
    if startOfRebound == true then
        print("Entity has started rebounding")
	else
        print("Entity has finished rebounding")
	end
end)

entity:SetCallback("OnDespawning", function()
    print("Entity is despawning")
    getgenv().IsAllRoomsFlickering = false
end)

entity:SetCallback("OnDespawned", function()
    print("Entity has despawned")
end)

entity:SetCallback("OnDamagePlayer", function(newHealth: number)
	if newHealth <= 0 then
		print("Entity has killed the player")
		local Players = game:GetService("Players")
		getgenv().MultiKilled = true
		
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")
local Remotes = ReplicatedStorage:WaitForChild("RemotesFolder")

local DeathTypes = {
	["Yellow"] = {"yellow", "curious"},
	["Blue"] = {"blue", "guiding"}
}

local function DeathHint(hints, deathType)
	if typeof(hints) ~= "table" or #hints == 0 then
		return
	end

	local MainUI = PlayerGui:WaitForChild("MainUI")
	local Death = MainUI:WaitForChild("Death")

	Death:GetPropertyChangedSignal("Visible"):Wait()

	deathType = tostring(deathType or "Guiding"):lower()

	local colour

	for name, values in next, DeathTypes do
		if table.find(values, deathType) then
			colour = name
			break
		end
	end

	if not colour then
		local MainGame = MainUI:WaitForChild("Initiator"):WaitForChild("Main_Game")
		local Health = MainGame:WaitForChild("Health")
		local Music = Health:WaitForChild("Music")

		for _, c in next, Music:GetChildren() do
			if c.Name:lower() == deathType then
				colour = c.Name
				break
			end
		end
	end

	colour = colour or "Blue"

	if firesignal then
		firesignal(
			Remotes.DeathHint.OnClientEvent,
			hints,
			colour
		)
	end
end

DeathHint({
"", "", "", "", "", "", "", ""
}, "Guiding")
	else
		print("Entity has damaged the player")
	end
end)

--[[

DEVELOPER NOTE:
By overwriting 'CrucifixionOverwrite' the default crucifixion callback will be replaced with your custom callback.

entity:SetCallback("CrucifixionOverwrite", function()
    print("Custom crucifixion callback")
end)

]]--

---====== Run entity ======---

entity:Run(true)
end
